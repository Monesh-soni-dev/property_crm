<header class="bg-white shadow-lg border-b border-gray-200 sticky top-0 z-50 backdrop-blur-md bg-white/95">
  <div class="container flex items-center justify-between px-6 py-4 mx-auto max-w-7xl">
    <!-- Logo -->
    <div class="logo">
      <%= link_to root_path, class: "flex items-center gap-4 group" do %>
        <div class="w-12 h-12 rounded-2xl bg-gradient-to-br from-blue-600 to-blue-700 shadow-xl flex items-center justify-center text-white font-bold text-lg transform group-hover:scale-110 transition-all duration-300">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
          </svg>
        </div>
        <div class="text-slate-900 text-2xl font-bold group-hover:text-blue-600 transition-colors duration-200 font-['Playfair_Display']">PlotPlans</div>
      <% end %>
    </div>

    <!-- Navigation -->
    <nav class="nav-links hidden lg:flex items-center gap-2">
      <%= link_to root_path, class: "flex items-center gap-2 text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-4 py-2.5 rounded-xl transition-all duration-200 font-medium group" do %>
        <svg class="w-4 h-4 group-hover:scale-110 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"></path>
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 21l8-4-8-4z"></path>
        </svg>
        Dashboard
      <% end %>
      
      <% if user_signed_in? %>
        <%= link_to leads_path, class: "flex items-center gap-2 text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-4 py-2.5 rounded-xl transition-all duration-200 font-medium group" do %>
          <svg class="w-4 h-4 group-hover:scale-110 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
          </svg>
          Leads
        <% end %>
      <% end %>
      
      <% if user_signed_in? && (current_user.admin? || current_user.builder?) %>
        <%= link_to properties_path, class: "flex items-center gap-2 text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-4 py-2.5 rounded-xl transition-all duration-200 font-medium group" do %>
          <svg class="w-4 h-4 group-hover:scale-110 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
          </svg>
          Properties
        <% end %>
        
        <%= link_to projects_path, class: "flex items-center gap-2 text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-4 py-2.5 rounded-xl transition-all duration-200 font-medium group" do %>
          <svg class="w-4 h-4 group-hover:scale-110 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
          </svg>
          Projects
        <% end %>
      <% end %>
    </nav>
    
    <!-- Auth Section -->
    <div class="cta flex items-center gap-3">

      <% if user_signed_in? %>
        <div class="flex items-center gap-4">
          <div class="flex items-center gap-3 bg-gray-50 rounded-2xl px-4 py-2.5 border border-gray-200">
            <div class="w-8 h-8 rounded-xl bg-gradient-to-br from-blue-500 to-blue-600 flex items-center justify-center text-white font-semibold text-sm shadow-sm">
              <%= current_user&.email&.first&.upcase || 'U' %>
            </div>
            <div class="hidden sm:block">
              <p class="text-sm font-semibold text-gray-900"><%= current_user&.full_name || current_user&.email&.split('@')&.first %></p>
              <p class="text-xs text-gray-500">Property Manager</p>
            </div>
          </div>
          <%= button_to destroy_user_session_path, method: :delete, class: "group flex items-center gap-2 bg-red-600 hover:bg-red-700 text-white px-5 py-2.5 rounded-xl font-semibold transition-all duration-200 shadow-lg hover:shadow-xl transform hover:scale-105 border-0 cursor-pointer" do %>
            <svg class="w-4 h-4 group-hover:rotate-12 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
            </svg>
            Logout
          <% end %>
        </div>
      <% else %>
        <div class="flex items-center gap-3">
          <%= link_to new_user_session_path, class: "text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-5 py-2.5 rounded-xl font-medium transition-all duration-200" do %>
            Sign In
          <% end %>
          <%= link_to new_user_registration_path, class: "bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-6 py-2.5 rounded-xl font-semibold transition-all duration-200 shadow-lg hover:shadow-xl transform hover:scale-105" do %>
            Get Started
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</header>